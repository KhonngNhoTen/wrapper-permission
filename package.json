{
  "name": "wrapper-permissions",
  "version": "1.0.0",
  "description": "generate swagger document for api (express, koa)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc -w && npm run build",
    "build": "rm -rf ./dist && tsc"
  },
  "keywords": [
    "auto-gen",
    "swagger"
  ],
  "author": "KhonngNhoTen",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}